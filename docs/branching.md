# Dependency Upgrade Branch

A dedicated branch named `chore/dependency-upgrades` has been created for the dependency update work.

To publish it to your GitHub remote, run:

```bash
git push -u origin chore/dependency-upgrades
```

After pushing, you can open a pull request from `chore/dependency-upgrades` into your default branch on GitHub.
